a.h () 
{ 
    local __T=a.h;
    : : Transforms the last command into a function;
    function - () 
    { 
        echo "$1 () {";
        shift;
        history 2 | head -1 | while read T T T REST; do
            echo "$REST";
        done;
        echo "}"
    };
    - $1 > $$;
    . $$;
    a.f $1;
    rm $$;
    : - "$1 () { $_T ; }";
    function - () 
    { 
        :
    }
}
