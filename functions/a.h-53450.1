a.h () 
{ 
    : a.h;
    : : Transforms the last command into a function;
    t=$(history 2 | head -1);
    cmd=$(- "$t" | while read first rest; do
        - $rest;
    done);
    cmd=$(
    - ": $1;";
    - $cmd;
);
    eval $1 '() {' $cmd'; }'
}
