a.hs () 
{ 
    function - () 
    { 
        echo $* | sed "^. /X/"
    };
    - $(history | tail -2 | head -1);
    _T=$(- $T | while read FIRST REST
    do
    - $REST
done 
    );
    : - "$1 () { $_T ; }"
}
