a.v () 
{ 
    function a.v. () 
    { 
        : this is my main repl;
        : editor;
        : because vim gets loaded;
        : into memory, it is;
        : really fast;
        : i suggest that you;
        : become a proficient;
        : typist and use vim
    };
    t=$(- $1 | sed 's/-.*$//');
    cp $(a.fs $t) $$.bash;
    for x in vim source rm;
    do
        $x $$.bash;
    done;
    - $t
}
