£ () 
{ 
    echo _$$
}
# example: set $(£) x
# OK, it does look like a bit like it is saying set sex
# but it is really creating a variable of underscore
# and the PID and setting that to x
# so the result would be
# > set $(£) x
# > echo $(£) 
# x
